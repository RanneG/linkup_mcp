# Copy these lines to Mac Hermes env (~/.hermes/.env or gateway shell).
# Replace PC host with your PC Tailscale IP when away from home LAN.

NAMI_BUILD_PC_URL=http://127.0.0.1:8770
NAMI_BUILD_TOKEN=paste-same-value-as-pc-dot-env

# After editing on Mac:
#   cd ~/Cursor/linkup_mcp && bash scripts/install-nami-hermes.sh && hermes gateway restart
