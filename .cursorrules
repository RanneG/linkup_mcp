# Cursor Linkup MCP Server

## Project Overview
This is a custom MCP (Model Context Protocol) server that integrates with Cursor IDE to provide:
1. **Web Search**: Deep web searches using Linkup API
2. **RAG (Retrieval Augmented Generation)**: Query documents using LlamaIndex with Ollama

---

## 🔗 Owner's GitHub Ecosystem

**GitHub Profile**: https://github.com/RanneG

### Reusable Libraries (Use These in New Projects!)

| Repository | Purpose | Install/Clone |
|------------|---------|---------------|
| **[chatbot-rag-core](https://github.com/RanneG/chatbot-rag-core)** | Reusable Python RAG chatbot library using Ollama (FREE, local AI) | `pip install git+https://github.com/RanneG/chatbot-rag-core.git` |
| **[chatbot-api-server](https://github.com/RanneG/chatbot-api-server)** | Production-ready Docker API server for chatbots | `git clone https://github.com/RanneG/chatbot-api-server.git` |

#### Quick Usage - chatbot-rag-core:
```python
from chatbot_rag import ChatbotAPI

chatbot = ChatbotAPI()
await chatbot.initialize("./documents")
response = await chatbot.chat("Your question here")
```

### Other Repositories

| Repository | Description | Tech |
|------------|-------------|------|
| **[linkup_mcp](https://github.com/RanneG/linkup_mcp)** | This repo - Cursor IDE MCP integration + `packages/stitch-api-types` for Stitch `/api` TS types | Python / TS |
| **standup-bot-backend** | Privacy-preserving standup aggregation (private) | Python |
| **standup-bot-frontend** | Standup bot demo UI (public) | — |
| **[MineSentry](https://github.com/RanneG/MineSentry)** | Mining/monitoring project | Python |
| **[Arbit](https://github.com/RanneG/Arbit)** | Arbitrage project | TypeScript |
| **[pixel-portfolio](https://github.com/RanneG/pixel-portfolio)** | Portfolio website | TypeScript |
| **[Country-Data-API](https://github.com/RanneG/Country-Data-API)** | CloudFormation IAC for deploying applications | AWS/CloudFormation |
| **[MSC-Final](https://github.com/RanneG/MSC-Final)** | VR/AR Masters dissertation project | VR/AR |

### When Building New Features:
- **Need a chatbot?** → Use `chatbot-rag-core`
- **Need production API?** → Use `chatbot-api-server`
- **Need Discord bot?** → Consider creating `discord-chatbot` repo using `chatbot-rag-core`
- **Need web search in Cursor?** → Use the `web_search` MCP tool
- **Need document Q&A in Cursor?** → Use the `rag` MCP tool

---

## Setup Instructions

### Prerequisites
- Python 3.12 or higher
- [uv](https://github.com/astral-sh/uv) package manager
- [Ollama](https://ollama.ai/) installed locally with llama3.2 model
- Linkup API key from https://www.linkup.so/ (optional - for web search)
- OpenAI API key (optional)

### Installation Steps

1. **Install dependencies**:
   ```bash
   uv sync
   ```
   Cursor MCP + RAG only. For **`stitch_rag_bridge.py`** / face / OAuth, use **`uv sync --extra stitch-bridge`**. For **`stitch_gui.py`**, add **`--extra stitch-gui`** as well.

2. **Set up environment variables**:
   Create a `.env` file with:
   ```
   LINKUP_API_KEY=your_linkup_api_key
   OPENAI_API_KEY=your_openai_api_key
   ```

3. **Install Ollama and pull the model**:
   ```bash
   # Install Ollama from https://ollama.ai/
   ollama pull llama3.2
   ```

4. **Configure Cursor to use this MCP server**:
   Add to `C:\Users\ranne\.cursor\mcp.json`:
   ```json
   {
     "mcpServers": {
       "linkup-server": {
         "command": "C:\\Users\\ranne\\AppData\\Local\\Microsoft\\WindowsApps\\python.exe",
         "args": ["-m", "uv", "run", "--directory", "C:\\Users\\ranne\\Cursor\\linkup_mcp", "python", "server.py"],
         "cwd": "C:\\Users\\ranne\\Cursor\\linkup_mcp"
       }
     }
   }
   ```

### Available MCP Tools

1. **web_search(query: str)**: Search the web using Linkup API
   - Returns sourced answers with citations
   - Supports standard and deep search modes

2. **rag(query: str)**: Query documents using RAG workflow
   - Currently configured for DeepSeek PDF in the data/ directory
   - Uses local Ollama model for LLM operations
   - Uses HuggingFace embeddings for vector search

### Project Structure
- `server.py`: Main MCP server implementation
- `rag.py`: RAG workflow using LlamaIndex
- `data/`: Document directory for RAG
- `pyproject.toml`: Project dependencies
- `.env`: Environment variables (not tracked in git)

### Development Notes
- The RAG workflow uses Ollama's llama3.2 model locally (FREE!)
- Documents in the `data/` directory are automatically indexed on server startup
- The server communicates via stdio protocol with Cursor
- Web search requires Linkup API key, but RAG works without any API keys






