# Standup Bot — Moved to Separate Repos

The Privacy-Preserving Standup Bot has been split into dedicated repositories:

- **standup-bot-backend** (private) — identity_core, processing_boundary, analytics_engine, API
- **standup-bot-frontend** (public) — Demo UI
- **sso-poc** (private) — Real U2SSO implementation

This repo (cursor_linkup_mcp) is now focused on the MCP server, RAG, and web search tools for Cursor IDE.
